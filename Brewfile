tap "hashicorp/tap"
tap "coursier/formulas"

# ─── Core CLI ─────────────────────────────────────────────────────────────────
brew "git"
brew "gh"
brew "glab"
brew "pre-commit"
brew "shellcheck"
brew "shfmt"          # shell formatter — paired with pre-commit hook
brew "hadolint"
brew "gitleaks"       # secret scanner — paired with pre-commit hook
brew "curl"
brew "unixodbc"

# ─── Productivity CLI (quality-of-life, used daily) ───────────────────────────
brew "jq"             # JSON wrangling
brew "yq"             # YAML/TOML wrangling
brew "fzf"            # fuzzy finder — pairs with Ctrl-R history search
brew "ripgrep"        # fast grep, used by most modern tools
brew "bat"            # `cat` with syntax highlighting
brew "eza"            # modern `ls` replacement
brew "git-delta"      # better `git diff` / `git log -p` output
brew "tree"
brew "mas"            # Mac App Store CLI — lets us automate store apps

# ─── Python ───────────────────────────────────────────────────────────────────
brew "python@3.10"
brew "python@3.11"
brew "python@3.12"
brew "python@3.13"
brew "uv"             # Python package manager / runner — replaces pip/venv
brew "hatch"

# ─── JVM ──────────────────────────────────────────────────────────────────────
brew "openjdk@11"
brew "openjdk@17"
brew "maven"          # JVM build tool (uses JAVA_HOME → openjdk@17)
brew "coursier/formulas/coursier"   # Scala artifact fetcher; used to install scalafmt


# ─── Infrastructure ───────────────────────────────────────────────────────────
brew "hashicorp/tap/terraform"
brew "terraform-docs"
brew "tflint"

# ─── Containers ───────────────────────────────────────────────────────────────
brew "podman"
brew "podman-compose"

# ─── Data platform ────────────────────────────────────────────────────────────
brew "databricks"

# ─── Zsh enhancements ─────────────────────────────────────────────────────────
brew "zsh-autosuggestions"
brew "zsh-completions"
brew "zsh-syntax-highlighting"

# ─── Casks (GUI apps) ─────────────────────────────────────────────────────────
cask "git-credential-manager"
cask "google-chrome"
cask "iterm2"
cask "pycharm"
cask "intellij-idea"
cask "alfred"
cask "chatgpt"
cask "claude"             # Anthropic Claude desktop app
cask "claude-code"        # Claude Code — agentic CLI from Anthropic
cask "podman-desktop"
cask "copilot-cli"
